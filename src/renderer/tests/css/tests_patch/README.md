# Patched CSS parsing tests

This directory contains complete replacement fixtures for files in
`../css-parsing-tests/` whose expected results target an older CSS Syntax
specification.

## TODO
Currently, css-parsing-tests serves as the primary test suite for the CSS parser. <br> 
As the parser evolves toward the current CSS Syntax Level 3 specification, this test suite will change to a newer, specification-aligned test suite.
